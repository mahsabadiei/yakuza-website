import { Inter } from "next/font/google";
import { Bai_Jamjuree, Waiting_for_the_Sunrise } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const baiJamjuree = Bai_Jamjuree({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});

export const sunrise = Waiting_for_the_Sunrise({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});
